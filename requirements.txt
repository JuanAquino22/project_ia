# Dependencias para sistema de transformación con Gradio
langchain==0.3.0
langchain-core
langchain-community==0.3.0
langchain-text-splitters==0.3.0

# Vector store (FAISS con embeddings hash-based)
faiss-cpu
numpy

# API para LLMs
requests

# Gradio para la interfaz (versión estable para Docker)
gradio==3.50.2

# Utilities
python-dotenv
